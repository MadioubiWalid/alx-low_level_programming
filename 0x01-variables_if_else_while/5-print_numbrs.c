#include <stdio.h>
/**
 * main - entry points
 * description - print basic numbers
 * return always 0
 */
int main(void)
{
	int n;

	for (n = 0,n <= 9, n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
